# Variables are used to store data
# PEP8: start variables with lowercase letters, use _ to separate words
# The = sign is the assignment operator. It is used to assign values to variables

complete_name = 'Renato Chagas de Moraes'
print(complete_name)

sum = 1 + 1
print(sum)

int_num = bool('1')
print(int_num)

print(type(complete_name), type(sum), type(int_num))
